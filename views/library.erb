<div class='jumbotron'>
  <h1> Library Database </h1>
</div>

<h2><a href='/authors'>Add a new author</a></h2><br>
<h2><a href='/books'>Add a new book</a></h2><br>

<h3>Here are all the authors in the database:</h3>

<% if @authors.any?() %>
  <p>Click on an author to see information about them and select the books they have written: </p>

  <ul>
    <% @authors.each do |author| %>
      <li><a href="/authors/<%= author.id() %>"><%= author.name() %></a></li>
    <% end %>
  </ul>
<% else %>
  <p>There are no authors added yet!</p>
<% end %>

<h3>Here are all the books in this database:</h3>

<% if @books.any?() %>
  <p>Click on a book to see information about it and mark which authors contributed to it:</p>

  <ul>
    <% @books.each do |book| %>
      <li><a href="/books/<%= book.id() %>"><%= book.title() %></a></li>
    <% end %>
  </ul>
<% else %>
  <p>There are no books added yet!</p>
<% end %>
